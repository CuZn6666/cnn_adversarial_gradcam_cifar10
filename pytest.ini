[pytest]
markers =
    requires_data: tests that require the real local CIFAR-10 dataset and are excluded from standard offline CI
    requires_cupy: tests that require CuPy, CUDA runtime access, and a visible CUDA GPU
